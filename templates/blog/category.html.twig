{% extends 'base.html.twig' %}

{% block body %}

<h2>{{ category.name }}</h2>

{% for article in articles  %}
    <p>{{ article.title }}</p>
    <p>{{ article.content }}</p>

{% endfor %}

{% endblock %}
