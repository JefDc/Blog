{% extends 'base.html.twig' %}

{% block title %}Index{% endblock %}


{% block body %}

    {#{{ form_start(form) }}#}
    {#{{ form_row(form.searchField) }}#}
    {#<button type="submit" name="btnSearch">Rechercher</button>#}
    {#{{ form_end(form) }}#}
    <div class="container">

    <h1 class="text-center mt-5">All articles from article s table : </h1>

    {% for article in articles %}
        <div class="mt-5 mb-5">
            <h2 class="">{#{{ loop.index }}#}  {{ article.title }}</h2>
            <h4 class="text-white-50"> {{ article.category.name }}</h4>
            <a href="{{ path('article_show', {'id': article.id}) }}" class="text-white">{{ article.content }}</a>
        </div>
    {% endfor %}
    <a href="{{ path('homepage') }}">
        Back to homepage.
    </a>
    <br>

    <br>
    <a href="{{ path('blog_index') }}">
        Testing index() method from BlogController to view all articles.
    </a>

    </div>
{% endblock %}